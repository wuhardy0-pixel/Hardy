# TASKS — Build plan

Work top to bottom. Don't start a phase until the previous one's gate is met.
Check boxes as you go and keep this file current — it is the project's source of truth
for status. Read `PRD.md` first.

## ⟵ CURRENT STATUS (2026-08-17)

Owner reviewed the local demo and approved it ("this demo is good"). Everything
through Phase 6 is built and demo-verified locally: 3-product catalog, per-part
colors + transparent, rotatable live preview with text/picture placement
(backs for ruler/crab), $2 add-on pricing, demo checkout, order-email content.

**Remaining to launch — all need the owner (agent guides each step):**
1. Vercel account + deploy → public link (Phase 2 leftover)
2. Stripe account + test keys → real checkout in test mode (Phase 5 leftover)
3. Resend account + key → order emails actually delivered (Phase 6 leftover)
4. Run docs/TESTING.md T1–T15 on the live link (Phase 7)
5. Live keys + one real refunded order → launch (Phase 8)

Owner's own printing to-dos (not launch-blocking, but before first lunchbox
sale): test-print the Lunchbox_Cap.stl hinge fit; replace renders with real
photos when convenient.

Order fulfillment tooling (2026-08-24): **tools/Lunchbox Lid Maker/** converts a
customer's picture + name into a lid with the image BAKED INTO the geometry
(pocket carved into the lid top, flush per-color inlay bodies; owner-requested
"baked in" style). Owner workflow: drop picture → edit order.txt → double-click
Make_Lunchbox_Lid.command → slice the output STLs as one multi-part object.
Tested end-to-end 2026-08-24 (render.png in each job folder shows the result).
The website preview now posterizes uploaded pictures to the opaque shop palette
so customers see the true printed look (chunky pixels, shop colors only).

## Phase 0 — Design approval  ✅ DONE
- [x] Create `design/mock.html` showing every screen (home, product/customizer, checkout, confirmation) and what the order email will look like
- [x] Owner approves the mock — approved 2026-08-14 ("it is good"). Build the UI to match the mock.

**Gate:** owner has explicitly said the mock is approved. Do not write app UI before this. ✅ Met.

## Phase 1 — Collect owner inputs (plain language, no technical questions)  ⟵ CURRENT
- [x] Per-product folders auto-created in `assets/products/` from her `Documents/3D` projects, photos copied (2026-08-14)
- [x] All 40 `info.md` files filled (2026-08-14, by agent per owner's instruction): fair prices set, custom text/image marked where they fit, multicolored flags, descriptions written from the actual photos. pill-pods (unfinished) and press-fit-box (no files exist) are "Sell this? no". Owner may adjust anything.
- [x] Owner filled `assets/products/shop-info.md` — shop name "Hardy's 3D"; colors Purple/White/Black/Blue/Orange/Red; flat $5 US shipping (confirmed 2026-08-14); about text drafted by agent, pending her tweaks
- [x] Preview renders generated in Blender for foosball-table, twist-gears, uno-case — but owner then ruled (2026-08-14) that unfinished-looking products are NOT for sale, so those three plus pill-pods and press-fit-box (no files exist) are all "Sell this? no". Owner then also excluded 4 products with gray render-only photos (dragons ×3, skeleton-guitarist) and asked the agent to cut likely non-sellers (2026-08-14): crab-gauge, dual-ruler, protractor, frog-purge-bucket, elephant-filament-bracket. Owner then removed 5 more by name, and finally (2026-08-14, latest) narrowed the launch catalog to **3 products: dual-ruler ($5), crab-gauge ($5), customizable-lunchbox ($30)**. Everything else is "Sell this? no — off the shelf for now"; flip to yes to restore any product (site rebuilds from the info.md files).

Late additions (2026-08-14 → 17, all built & demo-verified): per-zone color picking (lunchbox Box/Lid/Name-text; ruler & crab Body/Markings/Text) with only the first color required (others "Same"); rotatable 12-angle live preview with "Show back" flip for ruler/crab (their name + small picture print on the BACK, drawn in-preview with correct proportions); add-on pricing $2 each (text / picture / extra distinct color — PRD §4.2b); Transparent (clear) filament color; small-print warning for pictures on ruler/crab; instant local image preview + upload serving route; automatic image compression (37 MB → <1 MB) with loading shimmers. Preview assets generated by Blender scripts (session scratchpad); stored in assets/products/*/preview/frames/.
- [ ] Owner creates a Stripe account (guide her step by step at stripe.com; she needs her ID + bank details). Agent then retrieves/configures API keys — never ask her to handle keys

**Gate:** catalog data complete enough to build real product pages.

## Phase 2 — Project setup
- [x] Next.js app scaffolded in `app/` (2026-08-14); `npm run build` passes
- [ ] Deploy to Vercel so a public link exists ← needs a Vercel account/login
- [x] Catalog pipeline: `app/scripts/build-catalog.mjs` converts `assets/products/*/info.md` + `shop-info.md` → `data/catalog.json` + `public/products/` photos on every build

**Acceptance:** public link loads on phone and desktop. (Local: passes; public: pending deploy.)

## Phase 3 — Catalog & product pages
- [x] Homepage with product grid (31 real products, real photos, USD prices)
- [x] Product page per product, matching the approved mock
- [x] About section (from shop-info.md) on homepage

**Acceptance:** matches approved mock; owner confirms products/prices are correct. Owner review pending.

## Phase 4 — Customizer
- [x] Color swatches with shop-wide palette from `shop-info.md`, plus "Want a color you don't see?" request box on every product (goes into the order email)
- [x] Sizes: no current product has sizes; data field reserved (all "none")
- [x] Custom text field, 30-char max, live count, exact-text summary
- [x] Image upload only on products marked in `info.md` (JPG/PNG/HEIC ≤10 MB, thumbnail preview); optional unless info.md says "required"; Vercel Blob in production, local folder in dev
- [x] Validation with friendly messages; checkout blocked until required choices made (verified via API tests)

**Acceptance:** every rule in PRD §4.2 demonstrably works. ✅ (color-required, image-required, file-type/size checks tested 2026-08-14)

## Phase 5 — Checkout & payment (Stripe test mode)
- [x] Stripe Checkout code: name/email/phone/US shipping address collection, flat $5 shipping, customization in metadata; cancel returns to product with choices preserved (sessionStorage)
- [x] Demo mode (no Stripe key): full flow works without payment so everything is testable now — verified locally
- [ ] Verify with Stripe TEST keys once owner's Stripe account exists (test card 4242…, declined card)

## Phase 6 — Order email
- [x] Webhook `checkout.session.completed` → order email per PRD §4.4; email failure → 500 → Stripe auto-retries; orders always recoverable from Stripe dashboard
- [x] Customer confirmation email (best-effort)
- [ ] Verify real delivery to barbaratao@gmail.com once Resend account/key exists (until then emails log to server console — verified locally)
- [ ] Uploaded-image link in the email works from another device (needs deployed Blob storage)

**Acceptance:** email arrives within ~2 min of test payment and contains every field in PRD §4.4.

## Phase 7 — Testing
- [ ] Run every test in `docs/TESTING.md`; record results in that file
- [ ] Fix failures and re-run until all pass

**Gate:** all tests pass, including on a real phone.

## Phase 8 — Launch
- [x] ✅ Pokémon photo swapped (2026-08-14): lunchbox listing now shows original bunny+bear art (agent-drawn, Barbara's palette, saved at Documents/3D/Lunchbox/upload/Original_Badge_Art.png). No third-party characters anywhere on the site.
- [ ] Switch Stripe from test keys to live keys
- [ ] Owner (or agent with owner watching) places one real small order with a real card; verify email; refund it via Stripe
- [ ] Send the owner the final link and a short plain-language "how to read an order email & where to see payments" note
- [ ] Update README status to Launched

**Done when:** the PRD §2 success sentence is true on the live link.
